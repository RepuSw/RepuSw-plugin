---
description: Show project status including git state, recent changes, and pending tasks
---

Check and report the current project status:

1. Run `git status` to show working tree state
2. Run `git log --oneline -10` to show recent commits
3. Summarize any uncommitted changes and their scope
4. List any TODO or FIXME comments found in recently modified files

Present the information in a concise, organized format.
